<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home | Wanderlust Adventures</title>
    <link rel="stylesheet" href="/css/style.css" />
    <style>
        /* General Home Banner Styling */
        .home-banner {
            background-image: url('/images/homebanner.webp');
            background-size: cover;
            background-position: center;
            color: #ffffff;
            padding: 80px 20px;
            text-align: center;
            margin-bottom: 20px;
            margin-top: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            height: 400px;
            width: 85%;
            margin-left: auto;
            margin-right: auto;
        }

        .home-banner h2 {
            font-size: 3.75rem;
            margin-bottom: 10px;
            font-weight: 800;
            text-shadow: 2px 1.5px #333;
            font-family: ui-sans-serif, system-ui, sans-serif;
        }

        .home-banner p {
            font-size: 1.3rem;
            margin-bottom: 25px;
            text-shadow: 1px 1px #333;
            font-family: ui-sans-serif, system-ui, sans-serif;
        }

        .home-banner .explore-btn {
            font-size: 1.3em;
            background-color: #ffc107;
            color: #fff;
            padding: 12px 25px;
            border-radius: 5px;
            text-decoration: none;
        }

        .home-banner .explore-btn:hover {
            background-color: #e7b215;
            color: #000;
        }

        /* Animation on Page Load */
        @keyframes fadeSlideUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .home-banner h2,
        .home-banner p,
        .home-banner .explore-btn {
            opacity: 0;
            animation: fadeSlideUp 1s ease-out forwards;
        }

        .home-banner h2 {
            animation-delay: 0.2s;
        }

        .home-banner p {
            animation-delay: 0.4s;
        }

        .home-banner .explore-btn {
            animation-delay: 0.6s;
        }

        /* Responsive Mobile Adjustments */
@media (max-width: 768px) {
  .home-banner {
    width: 90%; /* Adjusted width for mobile */
    padding: 30px 15px; /* Reduced padding for text positioning */
    height: 250px; /* Adjusted height for mobile */
    margin-bottom: 10px;
  }

  .home-banner h2 {
    font-size: 1.8rem; /* Smaller font size for heading on mobile */
    margin-top: 10px; /* Adjusted margin to move the text upwards */
  }

  .home-banner p {
    font-size: 0.95rem; /* Reduced font size for paragraph on mobile */
  }

  .home-banner .explore-btn {
    font-size: 1.1em; /* Slightly smaller font size for button */
    padding: 8px 18px; /* Reduced padding for button */
  }
}

        /* Featured Adventures Section */
        .featured-adventures h2 {
            text-align: center;
            margin-bottom: 20px;
            font-size: 1.875rem;
            font-family: ui-sans-serif, system-ui, sans-serif;
            font-weight: 700;
            color: hsl(190 80% 45%);
        }

        .trips-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
        }

        .trip-card {
            border: 1px solid #ddd;
            border-radius: 8px;
            overflow: hidden;
            background-color: white;
            display: flex;
            flex-direction: column;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            transition: transform 0.2s ease-in-out;
        }

        .trip-card:hover {
            transform: scale(1.03);
        }

        .trip-card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
            display: block;
        }

        .trip-card h3 {
            padding: 10px;
            margin: 0;
            font-size: 1.2em;
            color: hsl(190 80% 45%);
        }

        .trip-card p {
            padding: 0 10px 10px;
            color: #666;
            flex-grow: 1;
        }

        .view-trip-btn {
            display: block;
            width: 100%;
            text-align: center;
            padding: 10px;
            background-color: hsl(190 80% 45%);
            color: white;
            text-decoration: none;
            border-radius: 0 0 8px 8px;
            box-sizing: border-box;
            transition: background-color 0.3s ease;
        }

        .view-trip-btn:hover {
            background-color: hsl(190, 78%, 30%);
        }

        /* Explore Banner Styling */
        .explore-banner {
            text-align: center;
            padding: 30px 20px;
            background-color: #ffc107;
            border-radius: 8px;
            margin: 20px 20px;
        }

        .explore-banner h2 {
            margin-bottom: 2px;
            font-size: 1.875rem;
            font-family: ui-sans-serif, system-ui, sans-serif;
        }

        .explore-banner p {
            margin-top: 1px;
        }

        .banner-buttons {
            margin-top: 20px;
        }

        .banner-buttons a {
            margin: 0 8px;
            font-size: 1em;
            text-decoration: none;
            color: #ffffff;
            background-color: hsl(190 80% 45%);
            padding: 12px 25px;
            border-radius: 5px;
        }

        .banner-buttons a:hover {
            background-color: #f8f9fa;
            color: hsl(190 80% 45%);
        }

        /* Responsive Explore Banner Button Adjustments */
        @media (max-width: 768px) {
            .explore-banner .banner-buttons a {
                padding: 10px 18px;
                /* Adjusted padding for buttons on mobile */
                font-size: 0.85em;
            }
        }
    </style>
</head>

<body>
    <%- include('partials/header') %>

        <div class="home-banner">
            <h2>Your Next Adventure Awaits</h2>
            <p>Discover breathtaking destinations and create unforgettable memories with Wanderlust Adventures.</p>
            <a href="/trips" class="explore-btn">Explore Trips</a>
        </div>

        <div class="container featured-adventures">
            <h2>Featured Trips</h2>
            <% if (featuredTrips && featuredTrips.length> 0) { %>
                <div class="trips-grid">
                    <% featuredTrips.forEach(trip=> { %>
                        <div class="trip-card">
                            <% if (trip.image) { %>
                                <img src="<%= trip.image %>" alt="<%= trip.name %>">
                                <% } %>
                                    <h3>
                                        <%= trip.name %>
                                    </h3>
                                    <p>
                                        <%= trip.description.substring(0, 100) %>
                                    </p>
                                    <a href="/trips/<%= trip._id %>" class="view-trip-btn">View Details</a>
                        </div>
                        <% }); %>
                </div>
                <% } else { %>
                    <p>Check back soon for featured adventures!</p>
                    <% } %>
        </div>

        <div class="explore-banner">
            <h2>Ready to Explore?</h2>
            <p>Let us help you plan your dream vacation. Browse our curated trips or contact us for a custom itinerary.
            </p>
            <div class="banner-buttons">
                <a href="/trips">View All Trips</a>
                <a href="/contact">Contact Us</a>
            </div>
        </div>

        <%- include('partials/footer') %>
</body>

</html>
