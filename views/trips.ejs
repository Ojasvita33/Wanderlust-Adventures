<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> | Wanderlust Adventures
    </title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<style>
    .trips-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 20px;
    }

    .trip-card {
        border: 1px solid #ddd;
        border-radius: 8px;
        overflow: hidden;
        background-color: white;
        display: flex;
        flex-direction: column;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        transition: transform 0.2s ease-in-out;
    }

    .trip-card:hover {
        transform: scale(1.03);
    }

    .trip-card img {
        width: 100%;
        height: 200px;
        object-fit: cover;
        display: block;
    }

    .trip-card h3 {
        padding: 10px;
        margin: 0;
        font-size: 1.2em;
        color: hsl(190 80% 45%);
    }

    .trip-card p {
        padding: 0 10px 10px;
        color: #666;
        flex-grow: 1;
    }

    .view-trip-btn {
        display: block;
        width: 100%;
        text-align: center;
        padding: 10px;
        background-color: hsl(190 80% 45%);
        color: white;
        text-decoration: none;
        border-radius: 0 0 8px 8px;
        box-sizing: border-box;
        transition: background-color 0.3s ease;
    }

    .view-trip-btn:hover {
        background-color: hsl(190, 78%, 30%);
    }
</style>

<body>
    <%- include('partials/header') %>

        <div class="container">
            <h1 style="color: hsl(190 80% 45%)">Explore Our Adventures</h1>
            <% if (trips && trips.length> 0) { %>
                <div class="trips-grid">
                    <% trips.forEach(trip=> { %>
                        <div class="trip-card">
                            <% if (trip.image) { %>
                                <img src="<%= trip.image %>" alt="<%= trip.name %>">
                                <% } %>
                                    <h3>
                                        <%= trip.name %>
                                    </h3>
                                    <p>
                                        <%= trip.description.substring(0, 100) %>
                                    </p>
                                    <a href="/trips/<%= trip._id %>" class="view-trip-btn">View Details</a>
                        </div>
                        <% }); %>
                </div>
                <% } else { %>
                    <p>No adventures available yet. Stay tuned!</p>
                    <% } %>
        </div>

        <%- include('partials/footer') %>
</body>

</html>